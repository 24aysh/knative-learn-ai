I configured Cursor Manually by adding the mcp.json -> i can do this using skills and initialize it

New MCP tools -> List available skills
				 Install available skills
					func mcp skill install
					    ↓
					detect supported agent/client
					    ↓
					find skill installation path
					    ↓
					copy knative-function-lifecycle/SKILL.md
					    ↓
					optionally update MCP client config
					    ↓
					verify skill is installed

					Available tools
				Example use-case -> func mcp skill install --client claude-code

Initial Setup -> install tools, make environment 


Create -> func create
Build -> func build
Deploy -> func deploy
List -> List available functions
Delete -> delete functions
MCP health check -> internal command (not func related)



Prerequiste check
	Local machine:
	    func installed?
	    git installed?
	    docker/podman available?
	    language runtime available?

	Cluster:
	    kubectl installed?
	    kubeconfig valid?
	    cluster reachable?
	    Knative Serving installed?
	    namespace exists?

	Registry:
	    registry configured?
	    credentials available?
	    image can be pushed?

	CI/CD:
	    git repo exists?
	    remote exists?
	    workflow directory exists?
	    required secrets known?

Read about builders -> s2i, host, plan


Test-func -> temp
Created the func
Building -> asks for docker registry
.git is not made when i make the function (ask it in the prompt whether there exists some way to it)
FUNC_ENABLE_CI_CONFIG=true func config ci -> generates github actions workflow
Then i need to push the code, manually add env variables like KUBECONFIG, REGISTRY Password -> at first glance, these might be new things for user
Knative serving is a bit tedious for new comers